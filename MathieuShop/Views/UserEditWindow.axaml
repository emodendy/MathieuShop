<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="MathieuShop.Views.UserEditWindow"
        Title="Пользователь"
        Width="420" Height="430"
        MinWidth="420" MinHeight="430"
        MaxWidth="420" MaxHeight="430"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        SystemDecorations="None"
        Background="Transparent">

    <Border CornerRadius="10" Background="#FFFFFF"
            BorderBrush="#E5E7EB" BorderThickness="1" ClipToBounds="True">
        <Grid RowDefinitions="Auto,*,Auto">

            <Border Grid.Row="0" Background="#F9FAFB"
                    BorderBrush="#E5E7EB" BorderThickness="0,0,0,1"
                    Padding="16,12" PointerPressed="TitleBar_PointerPressed">
                <Grid ColumnDefinitions="*,Auto,Auto">
                    <TextBlock Grid.Column="0" Name="WindowTitle" Text="Пользователь"
                               FontSize="14" FontWeight="SemiBold" Foreground="#111827"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Name="MinimizeButton" Content="—"
                            Width="28" Height="26" Padding="0"
                            Background="Transparent" Foreground="#6B7280"
                            BorderThickness="0" Click="MinimizeButton_Click"/>
                    <Button Grid.Column="2" Name="CloseButton" Content="✕"
                            Width="28" Height="26" Padding="0"
                            Background="Transparent" Foreground="#6B7280"
                            BorderThickness="0" Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Spacing="12" Margin="20,16">
                <Grid ColumnDefinitions="*,16,*">
                    <StackPanel Grid.Column="0" Spacing="5">
                        <TextBlock Text="Логин" FontSize="12" FontWeight="SemiBold"
                                   Foreground="#374151"/>
                        <TextBox Name="LoginBox" Height="36"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Spacing="5">
                        <TextBlock Text="Пароль" FontSize="12" FontWeight="SemiBold"
                                   Foreground="#374151"/>
                        <TextBox Name="PasswordBox" Height="36" PasswordChar="●"/>
                    </StackPanel>
                </Grid>
                <StackPanel Spacing="5">
                    <TextBlock Text="Полное имя" FontSize="12" FontWeight="SemiBold"
                               Foreground="#374151"/>
                    <TextBox Name="FullNameBox" Height="36"/>
                </StackPanel>
                <StackPanel Spacing="5">
                    <TextBlock Text="Email" FontSize="12" FontWeight="SemiBold"
                               Foreground="#374151"/>
                    <TextBox Name="EmailBox" Height="36"/>
                </StackPanel>
                <StackPanel Spacing="5">
                    <TextBlock Text="Роль" FontSize="12" FontWeight="SemiBold"
                               Foreground="#374151"/>
                    <ComboBox Name="RoleCombo" Height="36" HorizontalAlignment="Stretch"/>
                </StackPanel>
                <TextBlock Name="ErrorText" Text="" Foreground="#EF4444"
                           FontSize="12" IsVisible="False"/>
            </StackPanel>

            <Border Grid.Row="2" Background="#F9FAFB"
                    BorderBrush="#E5E7EB" BorderThickness="0,1,0,0"
                    Padding="20,12">
                <Button Name="SaveButton" Content="Сохранить"
                        Height="36" FontSize="13" FontWeight="SemiBold"
                        Background="#4F46E5" Foreground="White"
                        BorderThickness="0" CornerRadius="6"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Click="SaveButton_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
